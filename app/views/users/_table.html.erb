    <!-- Table -->
<div class="bg-base-200 rounded-lg border border-base-200 border-t-0 rounded-t-none">
  <table class="table table-zebra w-full">
    <thead>
      <tr>
        <th>Email</th>
        <th>Rola</th>
        <th>Data utworzenia</th>
        <th class="text-right">Akcje</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <%= render "row", user: user %>
      <% end %>
    </tbody>
  </table>
</div>