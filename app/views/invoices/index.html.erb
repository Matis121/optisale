<% content_for :title, "Faktury" %>
<%= render Ui::Layout::Container::DefaultComponent.new do %>
  <!-- Header -->
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-4">
    <div>
      <h1 class="text-2xl font-bold text-base-content">Faktury</h1>
      <p class="text-base-content/60 mt-1">Zarządzaj fakturami wygenerowanymi w systemie</p>
    </div>
  </div>
  <!-- Content -->
  <div class="bg-base-100 rounded-lg border border-base-300 shadow-sm">
    <%= turbo_frame_tag "invoices_table" do %>
      <%= render "table" %>
    <% end %>
  </div>
<% end %>